# Local Netlify folder
.netlify

# Dependencies
node_modules/

# Environment variables (contains secrets)
.env

# Generated config (built from env vars)
config.js

# Firebase service account key (secret — use FIREBASE_SERVICE_ACCOUNT env var)
*firebase-adminsdk*.json
appstore-images.zip
